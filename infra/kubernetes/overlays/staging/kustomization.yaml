apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: control-fabric

resources:
  - ../../base

configMapGenerator:
  - name: control-fabric-config
    behavior: merge
    literals:
      - LOG_LEVEL=info
      - ENVIRONMENT=staging
      - WORKER_CONCURRENCY=4
